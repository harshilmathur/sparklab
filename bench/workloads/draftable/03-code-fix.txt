Fix the bug in this Python function and return the corrected code in a single fenced block, then one sentence naming the bug. def merge_windows(windows): windows.sort(); out = [windows[0]]; [out.append(w) if w[0] > out[-1][1] else out.__setitem__(-1, (out[-1][0], w[1])) for w in windows[1:]]; return out  -- it must merge overlapping (start, end) tuples, but it corrupts results when a window is fully contained in the previous one.
