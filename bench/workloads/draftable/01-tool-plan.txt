You are an agent with these tools: web_search(query), read_file(path), write_file(path, content), run_shell(cmd). A user asks you to find every TODO comment in a Python repo at /srv/app, group them by module, and write a markdown report to /srv/app/TODO_REPORT.md. Respond ONLY with a JSON array of tool calls you would make, in order, each as {"tool": ..., "args": {...}, "reason": ...}.
